# 每日总结 — 电脑消费行为画像实训项目

项目仓库：`https://github.com/qinjuuu/small-study-term1`
项目路径：`C:\Users\Q\PycharmProjects\small study term1`

---

## 2026年6月29日（第一天）

今天实训课正式开始，早上尹艺霏老师通过腾讯会议开了快速会议，我专门查了一下当天的会议列表，确认尹老师的会议在运行中，对这次实训的课程要求有了基本了解。趁早上时间比较完整，我把桌面上那份37页的《01数据分析资料》PDF从头到尾看了一遍，这份资料是实训课程配套的学习材料，内容覆盖NumPy基础、NumPy索引与运算、pandas基础、pandas分组聚合与合并、Matplotlib和Seaborn可视化、综合实战、机器学习概述、线性回归、KNN分类和KMeans聚类，每个章节都有配套的代码示例。我把每个知识点都对应写了一个可独立运行的Python文件，一共八个，文件命名从02到09，每个文件都不依赖外部CSV文件，数据全部在文件内部自动生成，确保换一台电脑也能直接跑通，这八个文件作为今天打基础的全部输出，后续实训过程中可以随时翻出来参考。

选题这件事今天花了比较多的时间，我一开始列了八个可能的方向，包括员工离职预测、电信用户流失预测、心脏病预测、学生成绩影响因素分析、空气质量预测、电力负荷预测、超市销售分析和红酒质量分析，每个选项都认真权衡了数据可获取性、分析深度、模型可解释性和答辩时的展示效果。员工离职和电信流失这两个方向数据集比较成熟，但做的人太多，答辩时容易显得没有新意；心脏病和红酒质量的数据集样本量偏小，能做的分析维度有限；空气质量和电力负荷需要较多的时间序列处理，对实训项目的五天周期来说时间偏紧；超市销售分析的方向比较接近业务分析，但跟硬件消费关联不强。最终选了"学生学业成绩影响因素分析与预测"，数据来源是UCI ML Repository的Student Performance Dataset，包含数学科目395条和葡萄牙语科目649条，共33个特征，选这个方向是因为特征里有恋爱关系、饮酒行为、外出时间等比较生活化的变量，分析这些变量跟成绩的关系会比较有故事可讲，跟其他同学做的套路不太一样，答辩时不容易跟别人撞题。

定好选题之后，我从UCI下载了数据并解压到项目目录的data文件夹，获得了student-mat.csv和student-por.csv两个文件，注意到UCI的数据集是用分号分隔的，读取时必须指定sep=';'，这个细节后来写代码的时候一开始忘了，报了错才想起来。下载完数据之后，我创建了七个Python分析模块，文件名从01到07，分别覆盖数据探索、EDA可视化、特征工程、回归分析、分类分析、聚类分析和综合报告，每个模块都是独立可运行的脚本。特征工程部分做了几处比较用心的处理，比如构造了恋爱关系乘以外出时间的交互效应特征，理由是恋爱且经常外出的学生成绩可能受到叠加影响；还构造了社交活跃指数，把外出时间、课后社交时间等变量做了加权组合。今天最后生成了正式的选题表docx文档，文件名是"选题表_学生成绩影响因素分析.docx"，把选题背景、数据来源、分析思路、预期成果都写进去了，算是为实训项目完成了开题环节。

---

## 2026年6月30日（第二天）

今天项目方向做了一次比较大的调整。昨天定的学生成绩选题昨晚又想了想，觉得那个数据集只有一千来条，样本量偏小，而且特征虽然有趣，但能做的机器学习模型比较有限，主要是回归和分类，聚类的话效果也不一定好，整体分析深度可能不太够拿去答辩。今天跟佐佐木商量之后决定换题，新选题是"电脑消费行为画像——基于硬件性能与价格的用户群体细分"，这个方向数据可以自己模拟生成，样本量能做到八千条，分析维度也更丰富，既可以做回归预测，也可以做聚类细分，还能做推荐系统，整体来说更适合拿来做一个完整的实训项目。

确定方向之后就开始搭建项目结构，在PyCharm里创建了small study term1项目目录，同时把GitHub仓库初始化了，仓库地址是https://github.com/qinjuuu/small-study-term1，后续代码都往这个仓库里提交。数据集这块遇到了一个实际问题，原本打算用Kaggle的Laptop Price Dataset，但Kaggle的API怎么都连不上，查了一下应该是被墙了，挂不挂代理都不行，不想在这种事情上浪费太多时间，干脆改成自己模拟数据，写了src/generate_data.py脚本，用numpy的随机函数模拟生成了八千条笔记本数据，覆盖Basic、Student、Professional、Gaming四种使用类型，每种类型的硬件配置分布都做了差异化处理，比如Gaming类型的GPU性能分明显偏高，Professional类型的内存和存储偏大，确保每个产品的CPU核心数、CPU频率、内存容量、存储空间、CPU性能分、GPU性能分、综合性能分、价格等字段都有合理的数据分布，后续分析的数据基础算是打好了。

核心建模阶段今天跑了三个模型。线性回归模型用价格预测性能，R²是0.60，意味着价格能解释百分之六十的性能方差，这个结果其实挺合理的，因为性能定价本来就不是完全线性的，品牌溢价、市场定位这些因素都会让价格偏离纯性能导向，R²=0.60反而说明这个数据比较真实，不是那种过拟合的完美数据。KNN分类模型用来预测使用类型，K=6的时候准确率达到99.53%，几乎完美，这个结果说明不同使用类型的硬件配置差异确实非常明显，分类任务在这个数据上其实偏简单，因为四类产品的配置特征区分度很高。K-means聚类通过轮廓系数确认最优K=2，将产品划分为大众主流簇（占79%）和高性能发烧簇（占21%），这个比例跟现实市场是吻合的，大部分消费者买的是主流配置，真正追求极致性能的是少数。

今天还做了一次比较大规模的代码重构，这部分的出发点是原来的代码都写在一个models.py里，有330行，所有模型的代码挤在一个文件里，可读性比较差，后续加新模型也不方便。我把330行的单一models.py拆成了model_linear.py、model_knn.py、model_kmeans.py三个独立模块，每个模块只负责一个模型，结构清晰了很多。新建了src/config.py集中管理所有路径、随机种子、模型参数和调色板，后续改参数不用到每个文件里去找，统一在config.py里改就行。main.py也做了重构，原来的调用方式是用subprocess.run()去逐个调用各模块脚本，这种方式比较笨，而且各脚本的print输出不好捕获，改成直接函数导入调用，各模块都写了main()函数，在main.py里按顺序import之后逐个调用，现在一句python src/main.py就能跑通全流程，整个过程自动化了很多。除此之外还完善了requirements.txt，把项目依赖的包都列进去了，后续换环境装依赖比较方便，README.md也重新写了，把项目结构、核心结果、运行方式都写清楚了。到今天结束时，项目已经有13张图表输出到output/charts/目录，数据质量审计报告显示数据集中0条重复、0条缺失、0条业务不合理值，数据质量是可靠的。

---

## 2026年7月1日（第三天）

今天的重点是补上项目设计文档这一环，按照尹老师给的模板要求，实训项目需要先完成文字设计再动手编码，不能上来就跑模型，这个流程其实是比较规范的，先想清楚再动手可以避免后面频繁返工。我花了一整个上午梳理整个项目的设计思路，从项目背景出发，写的是在消费电子市场快速发展的背景下，硬件配置与价格之间的关系日益复杂，不同消费群体对性能和价格的敏感度存在明显差异，消费者在面对大量产品配置参数时往往难以做出最优决策，引入机器学习方法对电脑消费行为进行画像分析，既可以帮助消费者理解性能与价格的关系，也可以为厂商的产品定位提供数据支持，这个背景写出来比较有实际意义，不是为分析而分析。

项目目标部分明确了三个层次，第一层是构建性能-价格预测模型，用线性回归及其变种模型建立硬件配置到产品价格的映射关系，量化各硬件指标对价格的影响程度；第二层是基于聚类方法对产品进行用户群体细分，用K-means和GMM两种聚类方法，从不同角度发现产品集群的分布规律；第三层是设计推荐引擎辅助消费决策，结合用户的预算约束和使用需求，给出个性化的产品推荐列表，这三个层次是递进的，从理解规律到发现群体再到辅助决策，逻辑链条比较完整。

技术路线图部分我详细描述了从数据生成、数据质量审计、探索性数据分析、特征工程、模型训练、模型评估到结果可视化的完整流程，每一步都标注了所用的具体算法和技术细节，比如数据质量审计阶段检查重复值、缺失值和业务合理性三个维度，探索性数据分析阶段用分布图、箱线图、相关性热力图等多种可视化手段，特征工程阶段考虑是否需要对价格取对数处理，模型训练阶段用交叉验证来评估模型稳定性，这些细节写进去之后设计文档的技术含量会显得比较高。数据集介绍部分对模拟生成的八千条数据做了字段级别的说明，每个字段的含义、取值范围和数据分布特征都写了，比如CPU核心数的取值范围是2到16核，CPU频率的取值范围是1.8GHz到5.0GHz，内存容量取8GB、16GB、32GB、64GB四个离散值，存储空间取256GB到4TB之间的多个档位，这些细节能让老师看到数据生成过程是有认真设计的，不是随便乱造的数据。

今天最核心的输出是LRFMC模型设计，这个部分是我把传统的RFMC客户价值模型适配到了电脑消费场景里，重新定义了五个维度。L代表Lifecycle Level，用综合性能分Overall_Performance_Score来刻画产品生命周期层级，性能越高的产品代表越新的技术代际；R代表Resource Sensitivity，用性能除以价格也就是性价比来刻画资源敏感度，性价比越高的产品对价格敏感型消费者越有吸引力；F代表Function Intensity，用内存加存储加核心数来刻画功能强度，这个指标反映产品的综合功能配置水平；M代表Monetary，直接用价格Price_USD来刻画消费金额，是消费者预算约束的直接体现；C代表Class/Capability，用GPU与CPU性能比来刻画类别等级，这个比值高的产品偏向图形密集型应用场景，比值低的产品偏向通用计算场景。这五个维度合在一起构成一个五维特征向量，可以用来做聚类分析和用户画像，这个设计思路我没有在网上看到完全一样的，算是有一点自己的思考在里面。设计文档写完后按尹老师要求转成了Word格式，表格都完整保留，中文字体显示正常，整个文档结构清晰、内容详实，符合实训课程的设计文档规范，今天算是把设计这关过了。

---

## 2026年7月2日（第四天）

今天是整个实训项目里写代码最多、产出最密集的一天，创新模块集中爆发，项目的分析深度和图表数量都上了一个大台阶。早上开始先理了一下创新点应该放在哪里，在README.md里专门新增了创新方向章节，把这个项目和普通的课程大作业区分开来，核心思路是围绕"消费行为画像"这个主题设计多层次多角度的分析框架，而不是简单地调几个sklearn模型就完事，这个区别在答辩时老师是能看出来的，普通作业是"用了什么方法"，而这个项目要体现的是"为什么用这些方法"和"这些方法能解决什么业务问题"。

第一批新增的算法模块集中在回归和聚类的深度拓展上。写了src/model_linear_advanced.py，把基础线性回归扩展成了一个包含多元线性回归、Ridge回归、Lasso回归和XGBoost回归的五个模型对比框架，这个框架的思路是同一个预测任务用不同复杂度的模型各跑一遍，然后用横向条形图把各模型的R²、MAE和交叉验证R²放在同一张图上比较，可以直观地看到增加模型复杂度是否带来了性能提升，结果XGBoost的表现最好，R²比基础线性回归高了大约0.08，说明非线性关系和特征交互确实存在，但这个提升幅度不算特别大，说明线性关系在这个数据上仍然是主要的。还单独画了XGBoost的特征重要性Top10图，结果发现CPU性能分和GPU性能分是最关键的两个预测因子，这个结果符合直觉，因为CPU和GPU是电脑性能的核心决定因素，内存和存储的影响相对次要一些。

GMM高斯混合模型软聚类是今天比较有亮点的一个模块，K-means的优点是简单快速，但缺点是把每个样本硬性地分配到一个簇里，不给含糊的余地，而GMM可以输出每个样本属于各个簇的软概率，这个概率信息在业务上是有价值的，比如一个产品的簇归属概率是簇0占0.6、簇1占0.4，说明这个产品定位比较模糊，可能是厂商想通吃两个市场的结果。通过BIC和AIC准则自动选择最优组件数，不需要人工指定K值，这个比K-means更自动化了一点。还专门做了边界用户分析，把簇归属概率低于0.7的样本挑出来，这部分产品在业务上是最难做推荐决策的，模型给出了不确定性量化，这个是K-means没有的能力。

为了让所有模型有一个横向对比的基准，还写了src/model_compare.py，用雷达图和汇总条形图把回归五个模型和聚类三个模型放在同一个评价框架里展示，雷达图的五个维度是R²、MAE、准确率、轮廓系数和计算效率，这样老师一眼就能看出每个模型的优劣，不需要在各个章节之间来回翻。

下午开始做第二批创新模块，这部分更需要一点想象力，不能只停留在把课上讲的算法跑一遍。先实现了Isolation Forest异常定价检测，这个思路的来源是：如果能用回归模型预测每个产品的"公允价格"，那么实际价格和公允价格之间的残差就能用来判断定价是否合理，残差为正且幅度大的说明定价偏高，消费者在花冤枉钱，残差为负且幅度大的说明定价偏低，是性价比高的宝藏产品，这个分析结果对消费者做购买决策比较实用，而且Isolation Forest本身是一个无监督的异常检测方法，跟前面的有监督模型形成了一个方法上的对比。做这个模块的时候遇到了一个bug，Fair_Price（公允价格）为0的时候会导致直方图画出inf，因为做了除法，修复方法是在计算之前把Fair_Price为0的替换成1，同时对Residual_Pct做了[-500, 500]的截断处理，防止极端值把图表撑爆。

接着做了价格分箱分段分析，把产品按价格划分为Budget、Mid、Premium、Flagship四档，每档分别做回归分析，发现不同价格段的性能-价格关系模式是完全不同的，Budget档的性能价格几乎线性相关，Flagship档的性能价格关系开始出现平台期，说明高端产品的溢价因素更多，不是纯性能定价，这个结论如果写进报告里是一个有业务洞见的发现。然后做了品牌溢价量化分析，这个分析的思路是：控制配置变量（CPU性能分、GPU性能分、内存、存储）不变，用Ridge回归量化每个品牌的系数，系数为正且显著说明这个品牌有溢价能力，消费者愿意为这个品牌多付钱，系数为负说明这个品牌在打折竞争，这个分析角度在同类项目里比较少见到，因为需要先把配置的影响剔除掉，单独看品牌的效果，有一定的技术含量。

最后写了产品推荐引擎src/recommender.py，这个模块的输入是用户的预算上限和用途类型，系统综合三个维度打分：预算匹配度占40%权重，意思是推荐的产品价格不能离用户预算太远，最好是预算的80%到120%之间；性价比占30%权重，同等预算下性能越高的产品排名越靠前；类型匹配度占30%权重，比如用户选了Gaming类型，那么GPU性能分高的产品会获得额外加分。三个维度加权汇总后得到每个产品的推荐评分，取Top5输出，还支持命令行交互模式，可以直接在终端里输入预算和类型，看到推荐结果，这个模块是连接前面的所有分析模型和最终用户价值的一个桥梁，让整个项目有了一个应用出口。

除了算法模块，今天的另一个重头戏是把DIY配置推荐桌面工具从v1升级到了v2，v1其实写得比较简陋，就是几个滑块加一个列表框，v2我重新写了界面，用了暗色主题风格，整体视觉是深色极简、金色强调色、单列垂直布局，这个主题风格参考了游戏《明日方舟》的UI色调，做出来比较好看。组件选择改成优先级权重滑块，CPU、GPU、内存、存储、屏幕各有独立的0-100权重滑块，不需要强制加和为100，这样更灵活，因为不同用户对各个组件的重视程度确实不是均匀分配的，比如游戏用户可能GPU权重很高但内存权重很低，办公用户可能更看重CPU和内存。还内置了四个快捷预设方案，均衡型、游戏型（偏GPU）、办公型（偏CPU）和AI渲染型，一键加载预设之后再手动微调，提升使用效率。技术上实现了拖动滑块后300ms防抖自动刷新结果，防止滑块拖动过程中频繁触发计算导致界面卡顿；加了Ctrl+F快捷键触发搜索，可以在结果列表里快速筛选；双击结果行可以弹出详细配置弹窗，展示该产品的所有字段信息。价格显示也从USD改成了RMB，汇率用的是7.25，更符合国内用户习惯。还新增了屏幕尺寸SCR作为第五个优先级维度，评分公式里也相应加入了屏幕权重，因为屏幕尺寸对使用体验确实有影响，之前遗漏了这个维度。

今天的bug修复也没落下，model_linear.py里把x_line从numpy array改成了带列名的DataFrame，消除了sklearn在处理numpy array时发出的特征名警告，这个警告虽然不影响结果但比较烦人，消除之后输出更干净。brand_premium.py里把过时的labels参数改成了tick_labels，适配了新版matplotlib，旧参数在新版里会报DeprecationWarning。config.py里还加入了matplotlib的字体警告过滤，因为SimHei字体没有bold变体，matplotlib在绘制中文加粗标题时会发出警告，加了过滤之后终端输出清爽了很多。到今天结束时，项目总图表数从13张扩展到了30张，main.py也同步更新到了第11个阶段，本地全流程运行退出码0，所有模块都能正确执行，今天的产出是最多的。

---

## 2026年7月3日（第五天）

今天的核心目标是让项目从"跑出一堆静态图表"升级成"可以交互演示"，这个是答辩展示中最能体现项目完整度的部分，静态图表虽然也能说明问题，但交互式演示能让老师直观地看到你对数据的理解深度，也能让答辩过程更有参与感。我决定用Streamlit搭建一个完整的交互式仪表盘，把所有30张静态图表用Plotly重新实现一遍，Plotly的好处是生成的图表是SVG级别的矢量图，缩放不失真，而且原生支持悬停显示数值、框选缩放、平移等交互操作，比matplotlib的静态PNG好用很多。

整个app.py写了一个上午，最终实现了十个页面，覆盖数据概览、特征分析、回归分析、KNN分类、K-means聚类、GMM软聚类、异常检测、品牌溢价、推荐引擎和3D可视化，每个页面的结构基本是左侧是Plotly图表，右侧是Streamlit的交互控件，比如回归分析页面可以让用户通过多选框选择用哪几个特征做多元回归，系统实时重新训练模型并刷新结果图表；K-means页面可以让用户自己选择散点图的X轴和Y轴字段，想看什么维度之间的关系自己选；推荐引擎页面可以用滑块实时调整预算和使用类型，滑块一松手结果立即刷新，不需要点按钮。

为了让交互体验更丰富一点，今天还专门加了两个视觉效果比较好的功能。一个是K-Means聚类动画，用Plotly的动画框架把K-Means每一步迭代时质心是怎么移动的整个过程动态地展示出来，每一帧显示一次迭代后的质心位置，用较大的X标记标出质心，样本点用颜色区分所属簇，点播放按钮就能看到质心从初始位置逐步收敛到最终位置的全过程，这个动画在答辩时放出来效果应该不错，能直观地展示K-Means的工作原理，而不只是展示最终结果。另一个是3D可视化页面，用户可以选择任意三个维度做3D散点图，比如选CPU性能分、GPU性能分和价格作为三个轴，散点图可以用鼠标拖拽旋转，从不同角度观察数据分布，还加了PCA三维投影功能，把八维特征通过主成分分析压缩到三维空间，展示数据的整体分布形态，这个功能对高维数据的探索性分析比较有用。

全局交互方面，在侧边栏加了价格区间、使用类型、品牌的多选筛选器，所有页面的图表都会联动响应，比如在侧边栏把使用类型勾选为只显示Gaming，那么所有页面的数据和图表都只显示Gaming类型的产品，不需要每个页面单独设置筛选条件，这个联动机制是用Streamlit的session_state实现的，筛选后在右上角还会显示"当前展示X条/共Y条"的实时计数，让用户知道筛选掉了多少数据。界面美化也稍微弄了一下，写了自定义CSS，把标题做成渐变色，指标卡片加了阴影和圆角，侧边栏改成深色主题，整个仪表盘的视觉效果已经比较接近一个正规的数据产品了，而不是一个随手写的课程作业脚本。

今天的另一个任务是把所有的美元单位换成人民币，这个任务看起来简单但做起来比较繁琐，因为项目里美元的出现位置很多，图表hover标签里、坐标轴标签里、滑块标签里、指标卡片里都有。处理方式是先在real_analysis.csv里新增了Price_CNY列，用7.25的汇率把Price_USD全部转换过去，然后写了一个Python脚本批量处理app.py文件，把所有的USD、$、Price (USD)字符串替换成元、¥、价格(元)，包括Plotly的hovertemplate里的标签、滑块的label、指标卡片里的单位，确保答辩时老师看到的全部是人民币报价，这个细节处理完之后感觉整个项目的完整度又高了一截，因为老师看美元报价还需要在脑子里换算，直接看人民币更直观。

除了Streamlit仪表盘，今天还处理了diy_tool.py（桌面版）和网页版功能重复的问题，桌面版有组件权重滑块这个功能，网页版目前还没有，如果要完全用网页版替代桌面版，需要把组件权重这个功能也移植过去，这个任务今天没有完成，列在了待办事项里。到今天为止，整个small-study-term1项目已经具备了完整的分析深度、丰富的可视化形式和可交互的演示界面，从6月29日选题开始到今天7月3日，五个工作日把一个想法完整地落地成了一个可以拿去答辩的项目，该踩的坑基本都踩过了，该有的功能也都有了，后续主要是PPT制作、实训报告撰写和最后的代码整理了。

---

*记录整理：2026-07-03，基于各工作日真实聊天记录还原*
